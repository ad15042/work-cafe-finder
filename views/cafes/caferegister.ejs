<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>新規カフェ登録</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* 追加のスタイル */
        body {
            padding-top: 3rem;
        }
    </style>
</head>

<body>

    <div class="container">
        <h1 class="text-center mb-4">新規カフェ登録</h1>
        <div class="card">
            <div class="card-body">
                <form action="/cafe/register" method="POST">
                    <div class="form-group">
                        <label for="shopName">カフェの名前</label>
                        <input type="text" class="form-control" id="shopName" name="shopName"
                            placeholder="カフェの名前を入力してください">
                    </div>
                    <div class="form-group">
                        <label for="priceOfcoffee">コーヒー一杯の価格</label>
                        <input type="number" class="form-control" id="priceOfcoffee" name="priceOfcoffee"
                            placeholder="コーヒー一杯の価格を入力してください">
                    </div>
                    <div class="form-group">
                        <label for="location">住所</label>
                        <input type="text" class="form-control" id="location" name="location" placeholder="住所を入力してください">
                    </div>
                    <div class="form-group">
                        <label for="hasWiFi">WiFiの有無</label>
                        <select class="form-control" id="hasWiFi" name="hasWiFi">
                            <option value="true">あり</option>
                            <option value="false">なし</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="wifiStrength">WiFiの強度</label>
                        <input type="range" class="form-control-range" id="wifiStrength" name="wifiStrength" min="1"
                            max="100">
                    </div>
                    <div class="form-group">
                        <label for="comfort">快適さ</label>
                        <input type="range" class="form-control-range" id="comfort" name="comfort" min="1" max="10">
                    </div>
                    <div class="form-group">
                        <label for="comment">コメント</label>
                        <textarea class="form-control" id="comment" name="comment" rows="3"
                            placeholder="コメントを入力してください"></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary">登録する</button>
                </form>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS (optional) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>